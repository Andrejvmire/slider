export default class SliderController implements ISubscriber {
    private _model: IPublisher;
    private _view: IPublisher;

    constructor(options: OptionsType) {

    }

    update(data: ModelResponseType): void {
    }
}