type ViewOptionsType = {
    orientation?: SliderOrientationType,
    tooltip?: boolean,
    ruler: RulerType,
    points: PointsType,
    scale?: boolean | number[],
    ranger?: boolean | "revert",
}